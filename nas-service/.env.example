# Use the actual photo root on the NAS. The target path stays identical.
PHOTO_ROOT=/tmp/zfsv3/sata11/YOUR_NAS_ACCOUNT/data
HTTP_PORT=11280

# Required only for compose.image.yaml: directory containing config/config.json.
NAS_SERVICE_DIR=/replace/with/actual/service/directory
